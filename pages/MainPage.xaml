<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="rappel.MainPage">

    <VerticalStackLayout>
        <Grid RowDefinitions="*, Auto">
            <ScrollView Grid.Row="0" VerticalOptions="FillAndExpand" HeightRequest="640">
                <VerticalStackLayout>
                    <Label x:Name="testreponse"/>
                    <VerticalStackLayout x:Name="affmsg" Spacing="10">
                       
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </ScrollView>
        </Grid>
    </VerticalStackLayout>
</ContentPage>
